{
  "name": "ficta-monorepo",
  "private": true,
  "description": "Workspace root for ficta (packages/ficta) and its chat UI (apps/gateway). Not published.",
  "type": "module",
  "scripts": {
    "//turbo": "Turbo-managed: cached, parallel across all packages. See turbo.json",
    "build": "turbo run build",
    "check": "node scripts/sidecars.mjs check --advisory && pnpm check:static && pnpm check:sidecar-scripts && turbo run check",
    "check:static": "oxlint . && oxfmt --check .",
    "check:entity-fidelity-live": "pnpm --filter @serovaai/ficta check:entity-fidelity-live",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "//pnpm": "pnpm-orchestrated: interactive / write / dev servers, not cached",
    "check:fix": "oxlint --fix . && oxfmt --write .",
    "format": "oxfmt --write .",
    "format:check": "oxfmt --check .",
    "lint": "oxlint .",
    "dev": "node scripts/dev.mjs",
    "dev:proxy": "pnpm --filter @serovaai/ficta dev",
    "sidecars": "docker compose -f docker-compose.sidecars.yml --profile gateway --profile engine up -d --wait --build",
    "sidecars:openmed": "docker compose -f docker-compose.sidecars.yml --profile openmed up -d --wait",
    "sidecars:down": "docker compose -f docker-compose.sidecars.yml --profile gateway --profile engine --profile openmed down",
    "gateway:dev": "pnpm --filter @serovaai/ficta-gateway dev",
    "gateway:build": "pnpm --filter @serovaai/ficta-gateway build",
    "gateway:typecheck": "pnpm --filter @serovaai/ficta-gateway typecheck",
    "web:dev": "pnpm --filter @serovaai/ficta-web dev",
    "web:build": "pnpm --filter @serovaai/ficta-web build",
    "web:typecheck": "pnpm --filter @serovaai/ficta-web typecheck",
    "dev:all": "turbo run dev",
    "update": "pnpm -r update -i -L",
    "//release": "Releases are managed by Changesets. Author notes with `pnpm changeset`; CI (.github/workflows/publish.yml) opens a Version PR and, on merge, runs `release` to publish @serovaai/ficta + @serovaai/ficta-protocol together via npm OIDC.",
    "changeset": "changeset",
    "release": "changeset publish",
    "knip": "knip",
    "sidecars:check": "node scripts/sidecars.mjs check",
    "sidecars:update": "node scripts/sidecars.mjs update",
    "check:with-sidecars": "pnpm check",
    "check:sidecar-scripts": "node --test scripts/sidecars.test.mjs"
  },
  "devDependencies": {
    "@changesets/changelog-github": "^1.0.1",
    "@changesets/cli": "^3.0.3",
    "@types/node": "^26.6.2",
    "knip": "^6.37.0",
    "oxfmt": "^0.68.0",
    "oxlint": "^1.83.0",
    "turbo": "^2.11.2",
    "typescript": "^7.0.2"
  },
  "engines": {
    "node": ">=22.13"
  },
  "packageManager": "pnpm@12.5.1"
}
