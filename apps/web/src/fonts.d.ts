// Fontsource packages are imported for their CSS side effect only; they ship no type declarations
// for the bare-package import, so declare them as opaque modules to satisfy `tsc`.
declare module "@fontsource-variable/hanken-grotesk";
declare module "@fontsource/fragment-mono";
