---
"@serovaai/ficta": patch
---

Remove the unused `redactableBodyText` export — use `redactableBodyLeaves` (the same JSON string leaves, returned unjoined). Internal dead-code cleanup; redaction behavior is unchanged.
